{
  "name": "constantco-website-dev",
  "private": true,
  "version": "1.0.0",
  "description": "Constant & Co website — static front-end, Express API, automated tests",
  "scripts": {
    "test": "node tests/run-tests.cjs && vitest run --config vitest.config.mjs --reporter=verbose && node tests/print-test-footer.cjs",
    "test:vitest": "vitest run --config vitest.config.mjs",
    "test:basic": "node tests/run-tests.cjs",
    "test:api": "vitest run tests/api.integration.test.js --config vitest.config.mjs --reporter=verbose"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "nodemailer": "^8.0.7",
    "openai": "^6.37.0"
  }
}
